.container.articles
  .row
    .col-sm-12.col-md-12.col-lg-8.col-xl-8
      h1.my-4
        | Interesting articles
      - @articles.decorate.each do |article|
        .card.mb-4
          .card-body
            h2.card-title
              = link_to article.title, [:users, article]
            p.card-text
              == article.short_description
          .card-footer.text-muted
            = article.posted_date
            |  by 
            = article.author
      ul.pagination.justify-content-center.mb-4
        li.page-item
          a.page-link[href="#"]
            | ← Older
        li.page-item.disabled
          a.page-link[href="#"]
            | Newer →
    = render 'users/articles/sidebar'